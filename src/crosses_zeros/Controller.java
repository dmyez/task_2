package crosses_zeros;

import java.awt.*;

public class Controller {
    public void play() {
        Field field = new Field();
        View view = new View(field);
      //  Player first = new RandomComputerPlayere(Cell.CROSS, field);
  //      Player second = new NotSoRandomComputerPlayer (Cell.ZERO, field);


  //      Player currentPlayer = first;

        while (true) {
  //          System.out.println("Ходят" + currentPlayer.getSing(). getValue());
            view.showFild();
         //   Point coords = currentPlayer.makeTurn();
  //          if ( field.setCell(coords.x, coords.y, currentPlayer.getSing())) {

            }
        }

    }

//}
